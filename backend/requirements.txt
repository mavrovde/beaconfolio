fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic>=2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
sqlalchemy[asyncio]==2.0.54
asyncpg==0.31.0
pgvector==0.5.0
alembic==1.20.0
httpx==0.28.1
python-jose[cryptography]==3.5.0
# Direct dependency (issue #143): Fernet field-level encryption of the Gemini
# API key at rest. Already present transitively via python-jose[cryptography];
# pinned explicitly now that app.services.crypto imports it directly.
cryptography==50.0.1
passlib[bcrypt]==1.7.4
email-validator==2.3.0
python-multipart==0.0.32
Pillow==12.3.0
setuptools==84.0.0
# Local speech-to-text for the voice channel (#264). CTranslate2 inference on
# the CPU, in-process — RULE 10: no key, no endpoint, no metered call. The only
# network access is Hugging Face's anonymous, one-time model download into
# WHISPER_MODEL_DIR (a mounted volume, so it never repeats). Imported LAZILY
# inside app/services/transcription.py::_load_model, so nothing about it is
# loaded unless a voice message is actually transcribed.
faster-whisper==1.2.1


google-genai==2.24.0
respx==0.23.1
linkedin-api==2.2.1
